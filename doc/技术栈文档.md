# bm-cli 技术栈文档（v0.1）

## 1. 目标与约束

`bm-cli` 的目标是构建一个 Agent-First 的 Mermaid 渲染 CLI，核心要求如下：

- 优先服务 AI Agent（非交互、结构化协议）
- 支持终端直接显示（Unicode/ASCII）
- 支持中英文错误与消息
- 低资源占用，one-shot 执行后释放资源

## 2. 上游仓库事实依据

基于 `beautiful-mermaid` 仓库重分析得到：

- 核心能力已完备：`renderMermaid()` + `renderMermaidAscii()`。
- 技术基线为 `TypeScript + Bun + tsup`。
- 测试使用 `bun test`，CI 使用 Bun。
- 发布为 npm 包（ESM/CJS + browser IIFE）。

关键依据文件：

- `package.json`
- `tsconfig.json`
- `tsup.config.ts`
- `.github/workflows/ci.yml`
- `.github/workflows/publish.yml`
- `src/index.ts`
- `src/ascii/index.ts`

## 3. 推荐技术栈总览

| 层级 | 选型 | 角色 |
|---|---|---|
| 语言 | TypeScript（strict） | 统一类型系统与协议约束 |
| 运行时（开发/测试） | Bun | 快速启动、与上游一致 |
| 运行时（分发兼容） | Node.js 20 | npm 生态兼容与企业环境友好 |
| 渲染引擎 | beautiful-mermaid | 复用 SVG + ASCII 渲染能力 |
| CLI 入口 | 轻量参数解析（原生 `parseArgs` + 自定义） | 减少依赖与启动开销 |
| 协议校验 | JSON Schema + Ajv | `agent exec` 请求预校验与错误稳定化 |
| 语法规范层 | Mermaid Syntax Profile（JSON）+ 项目 Skill | 约束大模型语法输出并支持模式化校验 |
| CJK 宽度处理 | wcwidth 类库 | 中文终端对齐稳定 |
| 终端图形协议 | Kitty Graphics Protocol 编码器 | Ghostty/Kitty 图片直出 |
| SVG 光栅化 | `@resvg/resvg-js`（懒加载） | `image-kitty` 输出链路 |
| 构建 | tsup | 产出 ESM/CJS 与可执行入口 |
| 测试 | bun test + golden fixtures | 协议与渲染回归 |
| CI/CD | GitHub Actions（Bun + Node） | 分层校验与发布流程 |

## 4. 技术选型说明

### 4.1 TypeScript（严格模式）

- 与上游库一致，减少类型转换成本。
- 对 Agent 协议字段、错误码、schemaVersion 更容易做编译期约束。
- 适合维护长期演进的协议层。

### 4.2 Bun + Node 双运行时策略

- Bun 用于开发、测试、基准：速度快，和上游一致。
- Node 20 用于分发与兼容：保证 npm 用户环境可运行。
- 双栈避免锁死单运行时，兼顾效率与通用性。

### 4.3 轻量 CLI 层

- 不引入重型 CLI 框架，优先降低冷启动与依赖体积。
- 保持命令面简洁：`agent capabilities`、`agent exec`、`agent batch`、`term`。
- 默认 stdin/stdout，符合 agent pipeline 的组合式调用。

### 4.4 JSON Schema + Ajv

- 把“请求是否合法”前移到执行前。
- 支持 `errors[]` + `warnings[]` 双层校验策略。
- 为未来 schema 升级提供可验证边界。

### 4.5 CJK 宽度策略

- 中文在终端渲染存在宽度差异，必须引入 East Asian width 处理。
- 目标是同一图中中英文节点对齐稳定，避免边线错位。

### 4.6 渐进式终端渲染（Ghostty/Kitty 优化）

- 目标：在支持图形协议的终端中输出图片级 Mermaid 渲染，而不是退化为字符画。
- 渲染分级：
  - Level 0：ASCII（最大兼容）
  - Level 1：Unicode（字符画增强）
  - Level 2：`image-kitty`（终端图片直出）
- 关键技术：
  - 终端能力探测：优先协议探测，环境变量仅作辅助。
  - 光栅化：`renderMermaid()` 生成 SVG，`@resvg/resvg-js` 转 PNG。
  - 协议输出：手写轻量 Kitty 协议编码器，避免重型依赖。
- 回退策略：
  - `renderer=auto` 下，先尝试 `image-kitty`，失败回退 `terminal-unicode`，再回退 `terminal-ascii`。
  - 回退原因写入 `renderMeta.fallbackReason`。

### 4.7 Mermaid 语法规范化（Syntax Profile + Skill）

- 通过 `drafts/agent-protocol/syntax.profile.v2.draft.json` 固化五类图全量支持、规范化规则与路线策略（`strict|compat|recover`）。
- 通过 `specs/bm-cli-mermaid-syntax-v2.md` 提供人类可读规范。
- 通过 `specs/bm-cli-syntax-implementation-routes.md` 定义 Route1/Route2 的实现边界与验收。
- 通过项目 Skill `.claude/skills/bm-cli-syntax/SKILL.md` 约束大模型输出：
  - 默认 `normalized-v2 + strict`
  - 显式需求时可切换 `full-v2` 或 `recover`
  - 生成带 `syntaxProfile/syntaxMode` 的 `agent exec` 请求体

## 5. 建议工程结构

```text
bm-cli/
  src/
    cli.ts
    core/
      execute.ts
    commands/
      agent-capabilities.ts
      agent-exec.ts
      agent-batch.ts
      term.ts
    protocol/
      schema.ts
      errors.ts
      codecs.ts
    runtime/
      limits.ts
      locale.ts
      cjk-width.ts
      terminal-detect.ts
    adapters/
      mermaid.ts
      resvg.ts
      kitty-protocol.ts
  schemas/
    request.v1.schema.json
    response.v1.schema.json
  test/
    fixtures/
    golden/
  package.json
  tsconfig.json
  tsup.config.ts
```

## 6. CI 与发布建议

- CI 分层：
  - 协议层：schema 校验与类型检查
  - 渲染层：golden 输出比对
  - 工程层：格式化与基础 lint
- 发布产物：
  - npm 包（主渠道）
  - 可选 macOS 可执行文件（后续阶段）
- 发布清单：
  - `release-manifest.json` 记录版本、checksum、signature、最小兼容 schema

## 7. 备选方案与取舍

### 7.1 Go/Rust 重写 CLI

- 优点：单文件二进制更强。
- 缺点：需跨语言桥接 JS 渲染引擎，复杂度和维护成本高。
- 结论：当前阶段不采用。

### 7.2 直接使用重型 CLI 框架

- 优点：开发快。
- 缺点：依赖和启动成本增加，不利于 one-shot 低占用目标。
- 结论：优先轻量实现，必要时再引入。

## 8. 结论

当前最优路线是：

- 继续沿用 `TypeScript + Bun + tsup` 作为主工程栈
- 通过 `JSON Schema + Ajv` 强化 Agent 协议层
- 复用 `beautiful-mermaid` 现有渲染能力，不重写渲染核心
- 用双运行时策略（Bun 开发，Node 分发）平衡效率与兼容性

## 9. 优化建议（详细落地版）

本章基于当前会话结论与上游仓库真实问题，补充可执行优化清单。目标是把“设计原则”转成“开发任务 + 验收标准”。

### 9.1 优化总目标

- 提升终端渲染正确性，优先解决中文、日文、韩文与 emoji 宽度导致的错位问题。
- 提升协议可机器处理能力，减少 agent 依赖自然语言猜测行为。
- 提升产出稳定性，保证同输入同输出，便于回归测试与缓存复用。
- 提升发布可用性，支持 npm 与 Homebrew 的双轨分发。

## 10. P0 优化（必须优先完成）

### 10.1 CJK/Emoji 宽度统一

问题：

- 终端图渲染通常把 `string.length` 当显示宽度，中文和 emoji 在等宽终端中可能占 2 列，导致边框错位与文本溢出。

实施：

- 在 `bm-cli` 引入统一宽度模块：`src/runtime/cjk-width.ts`。
- 暴露函数：
  - `getDisplayWidth(text: string): number`
  - `sliceByDisplayWidth(text: string, maxWidth: number): string`
  - `padToDisplayWidth(text: string, width: number): string`
- 在终端输出路径中统一调用宽度函数，禁止直接依赖 `length` 做布局判断。
- 若上游库后续修复完成，保留兼容层并通过 feature flag 切换。

验收：

- 中文、日文、韩文、emoji、混排文本在 `terminal-unicode` 与 `terminal-ascii` 下边框不破裂。
- 同一输入在不同 shell 下宽度一致性可接受（zsh/bash）。

### 10.2 结构化错误模型

问题：

- 仅返回文本错误不利于 agent 自动修复，必须提供结构化诊断。

实施：

- 标准响应结构扩展为：
  - `error.code`
  - `error.messageZh`
  - `error.messageEn`
  - `error.details`
  - `error.range`（可选，行列信息）
  - `error.suggestion`（可选）
- 成功响应也支持 `warnings[]`，用于非阻断提示。
- 建立错误码字典文件：`src/protocol/errors.ts`，统一映射。

验收：

- 所有失败路径均返回稳定错误码。
- 解析错误可返回 `range` 时必须包含，无法定位时显式置空并给 `details.reason`。

### 10.3 `term` 与 `agent` 执行核心统一

问题：

- 两套执行逻辑容易行为漂移，导致同一输入在两命令下结果不一致。

实施：

- 抽取单一执行内核：`src/core/execute.ts`。
- `agent exec` 与 `term` 仅做输入解析与输出视图适配。
- 统一 limit 检查、超时控制、错误映射、输出后处理。

验收：

- 相同 Mermaid 输入在 `bm term` 与 `bm agent exec` 对应格式下内容一致。
- 错误行为一致（同错误码、同退出码）。

### 10.4 确定性输出

问题：

- 非确定性输出会破坏回归测试、缓存策略和 agent 自检。

实施：

- 默认关闭随机 ID、时间戳等非必要动态字段。
- JSON 输出字段顺序固定（编码层统一）。
- 可选添加 `--deterministic`，但默认行为即确定性。

验收：

- 同输入在同版本环境中多次运行输出字节级一致（排除耗时字段）。

## 11. P1 优化（协议与能力契约）

### 11.1 响应体改为判别联合（discriminated union）

问题：

- 同时返回 `terminal` 和 `svg`（含 `null`）会增加调用方分支复杂度。

实施：

- 统一数据模型：
  - `data.format`: `terminal-unicode | terminal-ascii | svg`
  - `data.content`: `string`
- 仅返回请求对应格式，禁止返回无关空字段。

验收：

- 响应中不再出现 `svg: null` 或 `terminal: null` 占位字段。

### 11.2 字段语义冲突规则固化

问题：

- `output.format` 与 `options.charset` 语义可能冲突。

实施：

- 规则固定：
  - `output.format` 是最终输出形态的唯一来源。
  - `charset` 仅在 `output.format=terminal-*` 时允许出现。
  - `font` 仅对 `svg` 生效；终端输出遇到 `font` 返回 warning。
- 在 Schema 层加互斥/依赖约束。

验收：

- 冲突输入能被稳定识别并返回 `E_INVALID_OPTION` 或 warning。

### 11.3 `capabilities` 升级为运行时契约

问题：

- 文档描述无法替代机器可读取能力声明。

实施：

- `agent capabilities` 必须包含：
  - `schemaVersion`
  - 支持图类型、输出格式
  - 默认 limits 与最大 limits
  - 引擎版本（`beautiful-mermaid`）
  - 已知限制列表（兼容语法/布局限制）
  - `terminal.features`（如 `kitty-graphics`、`true-color`）
  - `renderModes`（`ascii`、`unicode`、`image-kitty`）
- 新增命令或行为变更必须同步更新该输出。

验收：

- 调用方可仅依赖 `capabilities` 完成动态适配，无需查阅文档。

## 12. P2 优化（可扩展性与质量提升）

### 12.0 终端图形渲染增强（Ghostty/Kitty）

实施：

- 新增 `--renderer auto|text|image`。
- `auto` 默认行为：
  - 协议探测成功：`image-kitty`
  - 探测失败：`terminal-unicode`
  - 字符能力受限：`terminal-ascii`
- `@resvg/resvg-js` 按需加载，仅在图片模式加载。
- 增加资源保护：
  - `maxImagePixels`
  - `maxImageBytes`
  - `rasterizeTimeoutMs`

验收：

- Ghostty/Kitty 场景可直接显示 Mermaid 图片。
- 非 TTY/CI/pipe 默认文本回退，且回退原因可观测。

### 12.1 布局引擎抽象

实施：

- 抽象接口：`LayoutEngine`。
- 默认实现仍用 `dagre`，后续可实验 `elkjs`。

验收：

- 不改上层协议的前提下可替换布局引擎实现。

### 12.2 可观测性

实施：

- `meta.trace` 增加阶段耗时：parse/layout/render/serialize。
- 聚合统计：成功率、超时率、错误码分布。

验收：

- 能用指标定位性能瓶颈与高频失败场景。

## 13. 测试策略（与问题清单绑定）

### 13.1 Golden Fixtures（P0 必备）

- 终端宽度用例：
  - 纯中文节点
  - 纯日文节点
  - 纯韩文节点
  - emoji 节点
  - 中英混排节点
- 布局回归用例：
  - 多边汇聚到同一状态
  - loop/回路场景
  - ER 外键线重叠场景
- 语法兼容用例：
  - flowchart 分支 label 的不同写法
- 终端图形用例：
  - `renderer=auto` 在支持协议环境下选择 `image-kitty`
  - 协议不可用时稳定回退并返回 `fallbackReason`

### 13.2 协议回归

- `request/response schema` 验证。
- 冲突参数组合验证。
- 错误码稳定性验证（snapshot）。

## 14. CI/CD 细化

### 14.1 CI 分层任务

- `contract-check`：
  - Ajv schema 校验
  - TypeScript 类型检查
- `render-regression`：
  - golden fixtures 对比
  - CJK/emoji 专项用例
- `quality-check`：
  - 格式化与基础 lint

### 14.2 发布任务

- 构建 npm 包。
- 构建 macOS 二进制（arm64/x64）。
- 构建图形渲染兼容包（含 `resvg` 运行时验证）。
- 生成 `release-manifest.json`：
  - version
  - checksum
  - signature（可选）
  - minCompatibleSchema
  - renderModes

## 15. Homebrew 分发接入

本项目已增加 `homebrew` 模板目录，采用独立 tap 模式：

- `homebrew/Formula/bm-cli.rb`
- `homebrew/README.md`
- `homebrew/update_formula.sh`

后续动作：

- 统一命名到 `bm-cli`（若最终命名确定）。
- 发布时更新 Formula 的 `version` 与 `sha256`。
- 验证安装链路：
  - `brew tap <owner>/tap`
  - `brew install <owner>/tap/bm-cli`
- 验证图形渲染链路：
  - 在 Ghostty/Kitty 中执行 `bm-cli term --renderer auto`，确认可触发图片模式。

## 16. 命名一致性要求（立即修正）

若后续出现历史命名残留，需在发布前统一为 `bm-cli`：

- 仓库名
- npm 包名
- 可执行文件名
- Formula 名称
- 文档标题与路径

统一后再冻结 v1 协议，避免后期全链路重命名成本。
